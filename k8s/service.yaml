apiVersion: v1
kind: Service
metadata:
  name: flask-taskmanager-service
spec:
  selector:
    app: flask-taskmanager
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000